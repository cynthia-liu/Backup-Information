git

1. git config --global user.email "email.name"

2. git config --global user.name ""

3. ssh-keygen -t rsa -C "email.name"

4. cd ~/.ssh

5. cat id_rsa.pub

6. git clone -b (branch)--recurse-submodules http:(git)
